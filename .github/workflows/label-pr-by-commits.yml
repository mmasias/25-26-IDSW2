name: Analyze PR Commit Pattern

on:
  pull_request_target:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  pull-requests: write
  contents: read

jobs:
  analyze-commits:
    runs-on: ubuntu-latest
    name: Analyze commit pattern and label PR
    steps:
      - name: Get PR commits
        id: get_commits
        uses: actions/github-script@v7
        with:
          script: |
            const { data: commits } = await github.rest.pulls.listCommits({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
              per_page: 100
            });

            const dates = new Set();
            commits.forEach(commit => {
              const rawDate = commit.commit.author.date || commit.commit.committer.date;
              if (rawDate) {
                const date = new Date(rawDate);
                if (!isNaN(date.getTime())) {
                  dates.add(date.toISOString().split('T')[0]);
                }
              }
            });

            const uniqueDays = dates.size;
            const totalCommits = commits.length;

            console.log(`Total commits: ${totalCommits}, Unique days: ${uniqueDays}`);

            let label;
            let color;
            let description;

            if (uniqueDays >= 6) {
              label = 'sostenido-excelente';
              color = '0e8a16';
              description = '✅ Trabajo diario constante (6+ días)';
            } else if (uniqueDays >= 4) {
              label = 'sostenido-bueno';
              color = 'c2e0c6';
              description = '✅ Buen trabajo sostenido (4-5 días)';
            } else if (uniqueDays === 3) {
              label = 'sostenido-parcial';
              color = 'ffcc33';
              description = '⚠️ Trabajo parcialmente sostenido (3 días)';
            } else {
              label = 'no-sostenido';
              color = 'ff4444';
              description = '❌ Trabajo no sostenido (1-2 días)';
            }

            core.setOutput('label', label);
            core.setOutput('color', color);
            core.setOutput('description', description);
            core.setOutput('unique_days', uniqueDays.toString());
            core.setOutput('total_commits', totalCommits.toString());

      - name: Add label to PR
        uses: actions/github-script@v7
        with:
          script: |
            const label = '${{ steps.get_commits.outputs.label }}';
            const description = '${{ steps.get_commits.outputs.description }}';
            const uniqueDays = '${{ steps.get_commits.outputs.unique_days }}';
            const totalCommits = '${{ steps.get_commits.outputs.total_commits }}';

            const oldLabels = [
              'sostenido-excelente',
              'sostenido-bueno',
              'sostenido-parcial',
              'no-sostenido'
            ];

            const currentLabels = context.payload.pull_request.labels.map(l => l.name);

            for (const oldLabel of oldLabels) {
              if (currentLabels.includes(oldLabel) && oldLabel !== label) {
                await github.rest.issues.removeLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: context.issue.number,
                  name: oldLabel
                });
              }
            }

            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              labels: [label]
            });

            const comment = `## 📊 Análisis de patrón de commits\n\n` +
              `- **Total de commits:** ${totalCommits}\n` +
              `- **Días únicos de trabajo:** ${uniqueDays}\n` +
              `- **Etiqueta:** ${description}`;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const botComment = comments.find(comment =>
              comment.user.type === 'Bot' &&
              comment.body.includes('Análisis de patrón de commits')
            );

            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: comment
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: comment
              });
            }
