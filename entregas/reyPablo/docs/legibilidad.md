|Categoría||Justificación||
|-|-|-|-|
|**Nombrado**|Ejemplo +|Los nombres de las clases Asignatura, Examen o Profesor cumplen a la perfección con la regla. Son sustantivos, empiezan por mayúscula y utilizan términos exactos del dominio del problema, lo que hace que cualquier persona que lea el código entienda inmediatamente qué entidades se están modelando.|[Enlace al repo](https://github.com/Pareyor/23-24-prg2-ep/tree/main/src/ExamenParcial/ReyOrtizPablo)|
||Ejemplo -|Los nombres de paquetes deben ser sustantivos y comenzar en minúsculas|[Enlace al PR](https://github.com/Pareyor/23-24-prg2-ep/pull/1)
||Ejemplo -|Se deben evitar las codificaciones y nombres como por ejemplo DNI se debería usar documentoIdentidad, algo más internacional.|[Enlace al PR](https://github.com/Pareyor/23-24-prg2-ep/pull/1)
|**Comentarios**||No realicé comentarios en los códigos|
|**Formato**|Ejemplo +|En la clase cine,los atributos están arriba, seguidos del constructor y luego los métodos. Cada bloque está separado por exactamente una línea en blanco, lo que facilita la lectura sin desperdiciar espacio ni amontonar el código|[Enlace al repo](https://github.com/Pareyor/23-24-prg2/tree/correccionIDSW2/entregas/reyPablo/Reto-003)|
||Ejemplo -|En la clase Visitante todo el bloque de la clase está desplazado hacia la derecha. Esto sugiere visualmente que la clase Visitante está dentro de otra estructura, cuando debería estar al nivel de la raíz del archivo.|[Enlace al PR](https://github.com/Pareyor/23-24-prg2/pull/1)
||Ejemplo -|A diferencia de la clase anterior, en la clase Ticket los métodos get y set están pegados unos a otros sin ninguna línea de separación. Esto hace que el código parezca un muro de texto difícil de escanear visualmente.|[Enlace al PR](https://github.com/Pareyor/23-24-prg2/pull/1)
|**Estándares**|Ejemplo +|El código sigue el estándar declarar las variables de instancia (cola, numClientes) y la constante (MAX_COLA) al principio de la clase. |[Enlace al repo](https://github.com/Pareyor/24-25-EDA1/tree/correccionIDSW2/entregas/ReyOrtizPablo/Reto-001)|
||Ejemplo -|Uso de caracteres no ASCII en identificadores (añadirCliente). Aunque Java permite técnicamente usar la letra ñ o tildes, la norma estándar dicta el uso exclusivo de caracteres ASCII. Cambiar añadirCliente por un término del dominio como agregarCliente|[Enlace al PR](https://github.com/Pareyor/24-25-EDA1/pull/2)
||Ejemplo -|Distribución de métodos, los métodos públicos (la API de tu clase) deben ir primero, y los métodos privados (ayudantes) deben ir al final para no interrumpir la lectura de la funcionalidad principal.|[Enlace al PR](https://github.com/Pareyor/24-25-EDA1/pull/2)
|**Consistencia**|Ejemplo +|Nomenclatura de métodos de entrada (askChar, askInt, askString), todas las funciones que solicitan datos al usuario empiezan por el prefijo ask seguido del tipo de dato. Esto hace que el código sea predecible; si alguien necesita pedir un booleano en el futuro, instintivamente sabrá que debe llamarse askBoolean.|[Enlace al repo](https://github.com/Pareyor/24-25-EDA1/tree/correcci%C3%B3n3IDSW2/entregas/ReyOrtizPablo/pyEdlin)|
||Ejemplo -|Gestión de la entrada por teclado (Múltiples Scanners),En askChar, askString y askInt, se crea un new Scanner(System.in) dentro de cada método. Es inconsistente e ineficiente. Lo preferible es tener una única instancia de Scanner(normalmente como una constante o variable de clase). Crear múltiples instancias de Scanner sobre el mismo System.in puede causar comportamientos erráticos al cerrar el programa o vaciar el buffer.|[Enlace PR](https://github.com/Pareyor/24-25-EDA1/pull/3)
||Ejemplo -|Dualidad en la estructura de datos (List vs Array) y Desuso de clases propias, el código define una clase History que trabaja con arrays de Strings (String[]), pero en la clase Edlin el documento se gestiona como una Lista (List<String>) y el historial se maneja con un Stack nativo de Java, esto es totalmente inconsistente, tienes una clase History pero no la usas y obligas al sistema a convertir o mezclar tipos de datos. El documento debería ser un array o la clase History debería aceptar listas.|[Enlace PR](https://github.com/Pareyor/24-25-EDA1/pull/3)
|**Código muerto**|Ejemplo +|En el método procesarSiguiente() cada línea dentro de este método tiene un propósito activo y demostrable. El if (numClientes > 0) asegura que la lógica no falle, el elemento se extrae, el bucle desplaza los elementos restantes de forma justificada y actualiza el contador. No hay líneas redundantes, y todo el código es 100% alcanzable e imprescindible para el sistema.|[Enlace al repo](https://github.com/Pareyor/24-25-EDA1/tree/correccionIDSW2/entregas/ReyOrtizPablo/Reto-001)|
||Ejemplo -|En el método abandonarCola(String cliente) en ejecutarSimulacion() genera un cliente usando clienteAleatorio(). Este método garantiza (por su bucle do-while) que el cliente generado no está en la cola. Luego, si accion == 2, llamas a abandonarCola(cliente). Como ese cliente no está en la cola, el condicional if (cola[i].equals(cliente)) dentro de abandonarCola jamás se evalúa como verdadero.|[Enlace al PR](https://github.com/Pareyor/24-25-EDA1/pull/1)
|**DRY**|Ejemplo +|En el método buscarEnDiccionario, dentro del método comprimir, necesitas saber si una secuencia existe en el diccionario en dos momentos distintos (para wc y para w). En lugar de escribir el bucle for y la comparación .equals() dos veces dentro de la lógica principal, se ha extraído a una función. Si mañana decides que el diccionario debe ignorar mayúsculas, solo cambias una línea en buscarEnDiccionario y todo el sistema se actualiza. |[Enlace al repo](https://github.com/Pareyor/24-25-EDA1/blob/Correcci%C3%B3n4IDSW2/entregas/ReyOrtizPablo/Reto-003/Comprimir.java)
||Ejemplo -|Los tres métodos de expansión (expandirArrayDiccionario, expandirArrayValores, expandirArraySalida) hacen exactamente lo mismo, comprobar si el array está lleno, duplicar su tamaño y copiar los elementos. La única diferencia es el tipo de dato (String, int, Par), como consecuencia, si decides que en lugar de duplicar el tamaño quieres aumentarlo de 10 en 10 para ahorrar memoria, tienes que modificar tres sitios diferentes. Esto complica enormemente el mantenimiento y es propenso a errores.|[Enlace PR](https://github.com/Pareyor/24-25-EDA1/pull/4)
|**YAGNI**|Ejemplo +|El diseño minimalista de la clase Node, la clase Node tiene exactamente lo que se necesita para que este programa funcione, un valor (String value) y un puntero al siguiente nodo (Node next).|[Enlace al repo](https://github.com/Pareyor/24-25-EDA1/tree/correccion5IDSW2/entregas/ReyOrtizPablo/Reto0710)|
||Ejemplo -|Los métodos huérfanos en la clase List (find, insertFront, deleteFront, clearList), si miras la clase Ejemplo, el programa solo inserta por el final (insertEnd), borra por el final (deleteEnd) y lista los elementos (listAll). Actúa casi como una Pila o Cola simple. Sin embargo, la clase List tiene métodos muy complejos como find() (que itera la lista dos veces para crear y rellenar un array de resultados) que no se usan en ningún sitio.|[Enlace al PR](https://github.com/Pareyor/24-25-EDA1/pull/5)


### ¿CUAL FUE LA CATEGORÍA MAS DIFÍCIL DE ENCONTRAR Y POR QUÉ?

En mi opinión la categoría más difícil para encontrar ejemplos de código fue la consistencia, ya que para saber si hay fallos en la consistencia de código tienes que meterte en tu propia mente hace dos años y formularte la pregunta, ¿A qué me refería aquí con esto? Eso es lo que más problemas me dio ya que no me acordaba de la gran mayoría de códigos que implementé.
